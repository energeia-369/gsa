Admin login passkey :-PLAYADMIN2026



CREATE DATABASE playarena_db;

 USE playarena_db;

CREATE TABLE users ( id BIGINT AUTO_INCREMENT PRIMARY KEY, full_name VARCHAR(100), email VARCHAR(100) UNIQUE, password VARCHAR(255),role VARCHAR(50));

CREATE TABLE products ( id BIGINT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(150), category VARCHAR(100),price DOUBLE,description TEXT,image_url VARCHAR(500),stock INT );

CREATE TABLE tournaments ( id BIGINT AUTO_INCREMENT PRIMARY KEY,tournament_name VARCHAR(150), sport_name VARCHAR(100), location VARCHAR(150),event_date VARCHAR(100),entry_fee DOUBLE,status VARCHAR(50));


CREATE TABLE orders ( id BIGINT AUTO_INCREMENT PRIMARY KEY, user_id BIGINT,total_amount DOUBLE, payment_status VARCHAR(50), order_status VARCHAR(50), order_date DATETIME );

SELECT * FROM users;

 SELECT * FROM products;

SELECT * FROM tournaments;


SELECT * FROM orders;


ALTER TABLE users ADD wallet_balance INT DEFAULT 0, ADD credits INT DEFAULT 0, ADD total_orders INT DEFAULT 0, ADD events_joined INT DEFAULT 0;


 CREATE TABLE event_registrations ( id BIGINT AUTO_INCREMENT PRIMARY KEY, team_name VARCHAR(100), captain_name VARCHAR(100), captain_contact VARCHAR(20), email VARCHAR(100), sport VARCHAR(50), team_category VARCHAR(50), team_members INT, notes TEXT, registration_fee DOUBLE, payment_status VARCHAR(50) );


SELECT * FROM event_registrations;



changes to be done 
login otp for all admin and users 


makea flowchart 


frontend
https://play-arena-sports-event-e-commerce.vercel.app/

